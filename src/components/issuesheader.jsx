import React from "react";
export default ({}) => {
  return (
    <header class="issues-header">
      <nav>
        <a href="#" class="active">
          Open
        </a>
        <span> | </span>
        <a href="#">Closed</a>
      </nav>
    </header>
  );
};
